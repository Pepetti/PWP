{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the sports tracker",
  "main": "index.js",
  "scripts": {
    "test": "nodemon --exec 'mocha -R min'",
    "api_test": "nodemon --exec 'mocha -g 'API_TESTING''",
    "create_test": "nodemon --exec 'mocha -g 'Creating_documents''",
    "delete_test": "nodemon --exec 'mocha -g 'Delete_test''",
    "read_test": "nodemon --exec 'mocha -g 'Read_test''",
    "update_test": "nodemon --exec 'mocha -g 'Update_test''"
  },
  "author": "Oskari Kotajärvi & Pirkka-Pekka Launonen",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.6",
    "mongoose-sequence": "^5.1.0",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "supertest": "^4.0.2"
  }
}
